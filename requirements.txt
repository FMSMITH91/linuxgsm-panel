# Runtime dependencies, pinned to exact versions for reproducible, auditable
# deploys (install.sh reinstalls from this file on every update). Bumps are
# proposed automatically by Dependabot (grouped pip updates) so upgrades stay a
# reviewed, deliberate step rather than whatever floats in at deploy time.
flask==3.1.3
flask-socketio==5.6.1
flask-login==0.6.3
flask-sqlalchemy==3.1.1
flask-wtf==1.3.0
pyotp==2.10.0
qrcode==8.2
paramiko==5.0.0
bcrypt==5.0.0
eventlet==0.41.1
cryptography==49.0.0
websocket-client==1.9.0
